@page
@model CityModel
@using VirobnichaFirma.DAL
@{
    ViewData["Title"] = "Coutry";
}

<h1>Редактирование</h1>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="City!.Id" />
            <div class="form-group">
                <label asp-for="City!.CityName" class="control-label"></label>
                <input asp-for="City!.CityName" class="form-control" />
                <span asp-validation-for="City!.CityName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="City!.CountryId" class="control-label"></label>
                <select asp-for="City!.CountryId" asp-items="Model.Countries" class="form-select"></select>
                <span asp-validation-for="City!.CountryId" class="text-danger"></span>
            </div>
            <p/>
            <p/>
            <p/>
            <div class="form-group">
                <input type="submit" class="btn btn-primary" value="Сохранить"/>
                <a href="cities" class="btn btn-secondary">Назад</a>
            </div>
        </form>
    </div>
</div>